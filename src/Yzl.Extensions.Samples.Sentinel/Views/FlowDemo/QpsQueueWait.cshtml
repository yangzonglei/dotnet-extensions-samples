@{
    ViewData["Title"] = "QPS × 直接 × 匀速排队（排队等待）";
}
<div style="font-family: sans-serif; max-width: 640px; margin: 40px auto; line-height: 1.7">
    <h1>限流演示：@ViewBag.Mode</h1>
    <p>资源 key：<code>flow:page:qps-queue-wait</code>（由 <code>[SentinelResource]</code> 手动覆盖）</p>
    <p>规则：<code>@ViewBag.Rule</code></p>
    <p>当前请求已通过限流检查并匀速放行（当前时间 <strong>@ViewBag.Time</strong>）。</p>
    <p>@ViewBag.Tip</p>
    <p>预计等待超过 <code>maxQueueingTimeMs=5000</code> 的尾部请求才返回 429（渲染 <code>Views/Shared/RateLimit1.cshtml</code>）。</p>
    <p><a href="/">← 返回首页</a></p>
</div>
