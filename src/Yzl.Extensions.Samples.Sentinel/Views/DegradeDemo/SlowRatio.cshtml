@{
    ViewData["Title"] = "慢调用比例熔断演示";
}
<div style="font-family: sans-serif; max-width: 640px; margin: 40px auto; line-height: 1.7">
    <h1>慢调用比例熔断演示</h1>
    <p>熔断策略：<strong>@ViewBag.Mode</strong></p>
    <p>规则：<code>@ViewBag.Rule</code>（资源 <code>degrade:slow-ratio</code>，由 <code>[SentinelResource]</code> 手动覆盖）</p>
    <p>本页故意耗时 400ms（当前时间 <strong>@ViewBag.Time</strong>）。</p>
    <p>@ViewBag.Tip</p>
    <p>熔断期间所有请求直接返回 429（<code>RateLimit.cshtml</code>），不再进入本页；恢复时间窗过后自动进入半开探测。</p>
    <p><a href="/">← 返回首页</a></p>
</div>
